language: python
python:
  - "3.8"

install:
  - pip install -r requirements.txt

script:
  - python ./manage.py migrate
  - python ./manage.py test keeper.tests

notifications:
  email:
    on_success: always # default: change
    on_failure: always # default: always
